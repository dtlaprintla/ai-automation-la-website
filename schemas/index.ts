// Sanity Schema Index
import page from './page'
import { servicesSection, statsSection, testimonialsSection, ctaSection, contentBlock } from './sections'

export const schemaTypes = [
  // Documents
  page,
  
  // Sections/Objects
  servicesSection,
  statsSection, 
  testimonialsSection,
  ctaSection,
  contentBlock
]